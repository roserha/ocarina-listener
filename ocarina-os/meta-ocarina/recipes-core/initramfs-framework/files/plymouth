#!/bin/sh
# initramfs splash module

plymouth_enabled() {
    return 0
}

plymouth_run() {
    echo "SPLASH: starting" > /dev/console
    /usr/bin/ocarina-splash &
    echo "SPLASH: started pid $!" > /dev/console
}

plymouth_stop() {
    killall ocarina-splash 2>/dev/null || true
}